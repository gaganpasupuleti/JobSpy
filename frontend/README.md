# JobSpy Alerts Lab — Frontend

React + Vite + Tailwind UI for the alerts lab.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

App: http://localhost:3002 (proxies `/api` to backend on port 8002).
