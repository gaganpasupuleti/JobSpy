[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "python /app/run_worker.py --once --limit 10"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
